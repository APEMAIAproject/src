name: Build and publish PMs prediction container

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'PMs_prediction/**'
      - '.github/workflows/pms-prediction-container.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/apemaiaproject/pms-prediction
          tags: |
            type=raw,value=latest
            type=raw,value=5.1.0
            type=sha,prefix=sha-
          labels: |
            org.opencontainers.image.source=https://github.com/APEMAIAproject/src
            org.opencontainers.image.description=APEMAIA PM10/PM2.5 inference over Puglia and Bari

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./PMs_prediction
          file: ./PMs_prediction/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
